# 最近发展区精准作业设计工具 — 项目方案

> ⚠️ **维护规则**：本项目的任何代码变动完成后，**必须同步更新此 MD 文件中对应章节的内容**。

---

## 一、项目概述

面向最近发展区（ZPD）理论的精准作业设计 Web 工具。教师选择知识点、作业类型和理论框架，系统调用 DeepSeek API 生成结构化的作业内容。支持 4 种作业类型：

| 作业类型 | 输出结构 | 有无对话交互 |
|---------|---------|------------|
| 潜能适配型 | 理论层级（Bloom 6 层 / SOLO 5 层）× ZPD 三区 | 无 |
| 认知支持型 | 解题阶段（波利亚 4 阶段 / 图尔敏 6 阶段）× ZPD 三区支架 | 无 |
| 动态交互型 | ZPD 三区智能体对话配置 | ✅ Step 6 单智能体对话 |
| 协作探究型 | ZPD 三区多角色协作配置 | ✅ Step 6 多角色自动编排对话 |

---

## 二、技术架构

```
┌──────────────────────────────────────────────────┐
│                   前端 6 步向导                      │
│  选知识点 → 选类型 → 选理论 → 提示词 → 作业 → 对话   │
│  LaTeX 渲染（MathJax 3）                            │
└────────────────────┬───────────────────────────────┘
                     │ HTTP (AJAX)
┌────────────────────▼───────────────────────────────┐
│              Flask (app.py)                          │
│  7 个 API 路由 + prompt_builder + homework_generator │
└────────────────────┬───────────────────────────────┘
                     │ HTTPS
┌────────────────────▼───────────────────────────────┐
│         DeepSeek API (OpenAI 兼容)                    │
│  POST https://api.deepseek.com/v1/chat/completions   │
│  Model: deepseek-chat                                │
└──────────────────────────────────────────────────────┘
```

---

## 三、目录结构

```
zpd-homework-tool/
├── .env.example
├── .gitignore
├── requirements.txt
├── setup.bat / setup.sh
├── 项目方案-最近发展区精准作业设计工具.md   ← 本文件
│
├── app.py                  # Flask 主应用 (7 个路由)
├── config.py               # 知识点、作业类型、理论层级、ZPD映射
├── prompt_builder.py       # 4种作业类型提示词模板
├── llm_client.py           # DeepSeek API 封装
├── homework_generator.py   # 结构化作业生成 + 解析
│
├── templates/
│   └── index.html          # 前端单页 (6 步向导)
│
└── static/
    ├── style.css           # 响应式样式
    └── app.js              # 前端状态管理、API调用、聊天逻辑
```

---

## 四、DeepSeek API 配置

API Key 存放在 `.env` 文件中：

```
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
```

`.env` 已加入 `.gitignore`，不会提交到版本控制。通过 `python-dotenv` 读取。

调用方式使用 OpenAI 兼容接口：

```python
from openai import OpenAI
client = OpenAI(api_key="...", base_url="https://api.deepseek.com/v1")
response = client.chat.completions.create(
    model="deepseek-chat", messages=[...], temperature=0.7, max_tokens=4096
)
```

---

## 五、API 路由设计

| 路由 | 方法 | 功能 |
|------|------|------|
| `/` | GET | 返回主页面 |
| `/api/config` | GET | 前端配置（知识点、作业类型、理论、API状态） |
| `/api/generate-prompt` | POST | 生成提示词（纯模板填充，不调AI） |
| `/api/generate-homework` | POST | 调用 DeepSeek 生成结构化作业 |
| `/api/regenerate-level` | POST | 单独重新生成某个层级 |
| `/api/check-api` | GET | 检查 API Key 配置状态 |
| `/api/chat` | POST | Step 6 多轮对话（见下方） |

### 5.1 `/api/generate-homework` — 4 种返回结构

**潜能适配型** (`type: "theory_levels"`)：

```json
{
  "homework": {
    "type": "theory_levels",
    "theory_id": "bloom",
    "zones": [
      {
        "zone_id": "distal", "zone_label": "远端发展区 — 基础入门",
        "student_profile": "得分率 < 0.3",
        "icon": "🚀",
        "levels": [
          {"id": "remember", "name": "记忆", "order": 1, "content": "..."},
          {"id": "understand", "name": "理解", "order": 2, "content": "..."}
        ]
      },
      { "zone_id": "proximal", "levels": [{"id":"apply",...}, {"id":"analyze",...}] },
      { "zone_id": "existing", "levels": [{"id":"evaluate",...}, {"id":"create",...}] }
    ],
    "levels": [...]
  }
}
```

**认知支持型** (`type: "cognitive_stages"`)：

```json
{
  "homework": {
    "type": "cognitive_stages",
    "question": "题干（LaTeX）",
    "stages": [
      { "id": "understand", "name": "理解问题", "order": 1,
        "zones": { "distal": {"content":"..."}, "proximal": {...}, "existing": {...} } }
    ]
  }
}
```

**动态交互型** (`type: "dialogue_config"`)：

```json
{
  "homework": {
    "type": "dialogue_config",
    "question": "作业题目",
    "zones": [
      { "zone_id": "distal", "icon": "🚀", "style": "教学式引导",
        "opening": "...", "rules": "...", "scaffold_path": "...", "evaluation": "..." }
    ]
  }
}
```

**协作探究型** (`type: "inquiry_config"`)：

```json
{
  "homework": {
    "type": "inquiry_config",
    "theme": "探究主题",
    "zones": [
      { "zone_id": "proximal", "icon": "🎯", "style": "引导式探究",
        "roles": [{"name":"...","content":"..."}],
        "stages": [{"name":"...","content":"..."}],
        "rules": "..." }
    ]
  }
}
```

### 5.2 `/api/chat` — 多轮对话

```json
// 请求
{ "system_prompt": "...", "history": [...], "message": "学生输入" }

// 响应
{ "reply": "AI 回复（LaTeX 渲染）" }
```

---

## 六、前端界面（6 步向导）

| 步骤 | 内容 |
|------|------|
| Step 1 | 选择学段/学科/知识点 |
| Step 2 | 选择作业类型（4 种卡片） |
| Step 3 | 选择理论框架 + 填写补充信息 |
| Step 4 | 生成并确认提示词（可编辑） |
| Step 5 | 查看/调整作业（结构化渲染，三区标签切换，LaTeX 渲染） |
| Step 6 | 对话交互（仅第三/四种，三区独立聊天，AI自动编排） |

### Step 5 按类型展示

- **潜能适配型**：ZPD 区分组卡片，每题卡片按区上色（灰/橙/绿），标签显示区名
- **认知支持型**：题干 + 阶段×三区并排卡片
- **动态交互型**：三区标签切换查看对话配置
- **协作探究型**：三区标签切换查看角色/阶段配置

### Step 6 对话交互

- 三区独立对话历史，标签切换
- 第三种：苏格拉底追问式一对一辅导
- 第四种：AI 自动选择角色、自动推进/回退阶段，前端只展示阶段指示器
- 顶部展示题目/探究主题

---

## 七、四种作业类型的 ZPD 输出规则

### 7.1 潜能适配型 — Bloom 6 层级 → 3 ZPD 区

| ZPD 区 | 理论层级 | 题数 |
|--------|---------|------|
| 远端发展区 | 记忆 + 理解 | 2 |
| 最近发展区 | 应用 + 分析 | 2 |
| 现有发展区 | 评价 + 创造 | 2 |

SOLO 同理（5 层级 → 3 区，关联结构为最近/现有共用）

### 7.2 认知支持型 — 阶段 × ZPD 区

| ZPD 区 | 支架风格 |
|--------|---------|
| 远端发展区 | 知识讲解型 |
| 最近发展区 | 逻辑帮助型 |
| 现有发展区 | 扩展思考型 |

### 7.3 动态交互型 — ZPD 三区对话风格

| ZPD 区 | 风格 | 策略 |
|--------|------|------|
| 远端 | 教学式引导 | 先讲后问，卡1次给答案 |
| 最近 | 苏格拉底追问 | 追问为主，卡2次给提示 |
| 现有 | 挑战式对话 | 反问拓展，卡3次给提示 |

### 7.4 协作探究型 — ZPD 三区角色规模

| ZPD 区 | 角色数 | 阶段数 |
|--------|-------|-------|
| 远端 | 2 | 1 |
| 最近 | 3 | 2 |
| 现有 | 5 | 3 |

---

## 八、Python 依赖

```
flask==3.1.0
openai==1.68.0
python-dotenv==1.1.0
```

---

## 九、从零克隆到运行

### 前提

- 安装 [uv](https://docs.astral.sh/uv/getting-started/installation/)
- 有 DeepSeek API Key

### 步骤

```bash
# 1. 克隆
git clone https://github.com/Z-BL/zpd-homework-tool.git
cd zpd-homework-tool

# 2. 一键初始化
bash setup.sh          # macOS/Linux
# 或双击 setup.bat     # Windows

# 3. 编辑 .env，填入 API Key
# DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx

# 4. 启动
.venv/bin/python app.py       # macOS/Linux
.venv\Scripts\python app.py   # Windows

# 5. 浏览器访问 http://localhost:5001
```

### 手动初始化（不用 setup 脚本）

```bash
uv venv
uv pip install -r requirements.txt
cp .env.example .env
# 编辑 .env 填入 API Key
.venv/bin/python app.py
```

---

## 十、更新已克隆的项目

```bash
cd zpd-homework-tool

# 拉取最新代码
git pull origin main

# 如果有新依赖（查看 requirements.txt 是否变化）
uv pip install -r requirements.txt

# 重启应用
.venv/bin/python app.py
```

---

## 十一、实施完成状态

| 文件 | 状态 |
|------|------|
| `app.py` | ✅ 7 个路由 |
| `config.py` | ✅ 学段/学科/知识点 + 4 作业类型 + ZPD映射 + 理论层级 + 支架风格 |
| `prompt_builder.py` | ✅ 4 种类型 × ZPD 结构化提示词 |
| `llm_client.py` | ✅ DeepSeek API 封装 |
| `homework_generator.py` | ✅ 4 种解析器（theory_levels/cognitive_stages/dialogue/inquiry） |
| `templates/index.html` | ✅ 6 步向导 + MathJax |
| `static/app.js` | ✅ 状态管理/聊天/LaTeX渲染 |
| `static/style.css` | ✅ 响应式 + ZPD三区色系 |

### 环境

- **Python**：3.13.14（uv 管理）
- **包管理**：uv
- **端口**：默认 5001（`PORT` 环境变量可配置）

---

> **最后更新**：2026-08-07
