# 最近发展区精准作业设计工具 — 项目方案

> ⚠️ **维护规则**：本项目的任何代码变动（功能新增、配置修改、Bug 修复等）完成后，**必须同步更新此 MD 文件中对应章节的内容**。确保其他协作者或 AI Agent 打开此文件即可了解项目最新全貌，无需翻阅代码。

---

## 一、项目概述

面向最近发展区（ZPD）理论的精准作业设计 Web 工具。教师选择知识点、作业类型和理论框架后，系统调用 DeepSeek 大模型 API 生成完整提示词，经教师确认后，为远端发展区、最近发展区、现有发展区三个层级分别生成差异化作业内容。教师可对生成的作业进行调整、重新生成等操作。

---

## 二、技术架构

```
┌──────────────────────────────────────────────────┐
│                    前端 (浏览器)                    │
│   index.html / app.js / style.css                 │
│   步骤式向导：选知识点 → 选类型 → 选理论 →         │
│   生成提示词 → 生成三层次作业 → 调整确认            │
└────────────────────┬─────────────────────────────┘
                     │ HTTP (AJAX)
┌────────────────────▼─────────────────────────────┐
│              后端 Flask (app.py)                   │
│   - 路由处理                                      │
│   - 提示词模板填充                                 │
│   - 调用 DeepSeek API 生成作业                     │
│   - 返回结构化 JSON                               │
└────────────────────┬─────────────────────────────┘
                     │ HTTPS
┌────────────────────▼─────────────────────────────┐
│          DeepSeek API (OpenAI 兼容)                │
│   POST https://api.deepseek.com/v1/chat/completions│
│   Model: deepseek-chat                            │
└──────────────────────────────────────────────────┘
```

---

## 三、目录结构

```
E:\desktop\1\
├── 知识点.md                          # 原始资料（已存在）
├── 四种作业形态.md                     # 原始资料（已存在）
├── 潜能适配型作业-提示词.md             # 原始资料（已存在）
├── 认知支持型作业-提示词.md             # 原始资料（已存在）
├── 动态交互型作业-提示词.md             # 原始资料（已存在）
├── 协作探究型作业-提示词.md             # 原始资料（已存在）
├── 项目方案-最近发展区精准作业设计工具.md  # 本方案文件（实时更新）
│
└── demo/                              # 所有代码在此文件夹下
    ├── .env                           # DeepSeek API Key 配置
    ├── .env.example                   # API Key 配置模板
    ├── requirements.txt               # Python 依赖
    ├── app.py                         # Flask 主应用
    ├── config.py                      # 知识点、作业类型、理论等结构化配置
    ├── prompt_builder.py              # 提示词构建模块（4种作业类型的模板填充）
    ├── llm_client.py                  # DeepSeek API 调用封装
    ├── homework_generator.py          # 三层次作业生成逻辑
    ├── templates/
    │   └── index.html                 # 前端单页界面
    └── static/
        ├── style.css                  # 样式
        └── app.js                     # 前端交互逻辑
```

---

## 四、DeepSeek API 配置

### 4.1 API Key 存放位置

```
demo/.env
```

内容格式：
```
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### 4.2 API 调用方式

DeepSeek 使用 OpenAI 兼容接口，通过 `openai` Python SDK 调用：

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-xxx",
    base_url="https://api.deepseek.com/v1"
)

response = client.chat.completions.create(
    model="deepseek-chat",
    messages=[
        {"role": "system", "content": "系统提示词..."},
        {"role": "user", "content": "用户提示词..."}
    ],
    temperature=0.7,
    max_tokens=4096
)
```

### 4.3 API Key 安全说明

- `.env` 文件已加入 `.gitignore`，不会提交到版本控制
- `.env.example` 提供模板文件，其他开发者可复制后填入自己的 Key
- 后端通过 `python-dotenv` 读取 `.env` 文件

---

## 五、API 路由设计

| 路由 | 方法 | 功能 | 说明 |
|---|---|---|---|
| `/` | GET | 返回主页面 | 渲染 index.html |
| `/api/config` | GET | 返回前端所需配置 | 知识点列表、作业类型及可选理论 |
| `/api/generate-prompt` | POST | 生成完整提示词 | 不调用 AI，纯模板填充 |
| `/api/generate-homework` | POST | 调用 DeepSeek 生成三层次作业 | 核心 AI 调用接口 |
| `/api/regenerate-level` | POST | 单独重新生成某个层级 | 传入层级名 + 修改要求 |

### 5.1 `/api/config` 返回结构

```json
{
  "knowledge_points": ["有理数", "实数", "整式及其加减", ...],
  "homework_types": [
    {
      "id": "potential_adaptive",
      "name": "潜能适配型",
      "description": "分析学情基础，生成递进任务",
      "output_form": "递进题目序列",
      "theories": [
        {"id": "bloom", "name": "布卢姆认知目标分类"},
        {"id": "solo", "name": "SOLO分类理论"}
      ],
      "supplement_fields": [
        {"key": "grade", "label": "年级", "type": "text"},
        {"key": "subject", "label": "学科", "type": "text"},
        {"key": "original_question", "label": "作业原题目", "type": "textarea"},
        {"key": "teaching_goal", "label": "教学目标", "type": "textarea"},
        {"key": "student_data", "label": "学生学情数据", "type": "textarea"}
      ]
    }
    // ... 其他三种类型
  ]
}
```

### 5.2 `/api/generate-prompt` 请求/响应

请求：
```json
{
  "knowledge_point": "一元二次方程应用",
  "homework_type": "potential_adaptive",
  "theory": "bloom",
  "supplement": {
    "grade": "八年级",
    "subject": "数学",
    "original_question": "某工厂今年1月份产值...",
    "teaching_goal": "能够根据实际问题建立一元二次方程模型",
    "student_data": "学生A: 85分；学生B: 60分；..."
  }
}
```

响应：
```json
{
  "prompt": "我是八年级数学的老师，正在设计一份基于最近发展区的精准作业...\n\n知识点是：一元二次方程应用\n\n请依据认知目标分类理论...",
  "system_prompt": "你是一位资深的数学教育专家..."
}
```

### 5.3 `/api/generate-homework` 请求/响应

请求：
```json
{
  "knowledge_point": "一元二次方程应用",
  "homework_type": "potential_adaptive",
  "theory": "bloom",
  "prompt": "（Step 4 确认后的完整提示词）",
  "system_prompt": "..."
}
```

响应（调用 DeepSeek 后）：
```json
{
  "homework": {
    "distal": {
      "label": "远端发展区",
      "description": "后继知识探索",
      "content": "...（AI生成的3道题目）..."
    },
    "proximal": {
      "label": "最近发展区",
      "description": "当前知识点的认知进阶",
      "content": "...（AI生成的3道题目）..."
    },
    "existing": {
      "label": "现有发展区",
      "description": "知识讲解+基础题",
      "content": "...（AI生成的知识讲解+1道基础题）..."
    }
  }
}
```

### 5.4 `/api/regenerate-level` 请求/响应

请求：
```json
{
  "knowledge_point": "一元二次方程应用",
  "homework_type": "potential_adaptive",
  "theory": "bloom",
  "level": "proximal",
  "adjustment": "题目难度再提高一些，增加一道应用题",
  "original_prompt": "（原始提示词）",
  "system_prompt": "..."
}
```

响应：同 `/api/generate-homework` 的单层级结构。

---

## 六、前端界面设计（5 步向导）

```
┌──────────────────────────────────────────────────────┐
│   🎓 面向最近发展区的精准作业设计工具                    │
├──────────────────────────────────────────────────────┤
│                                                       │
│  Step ① → Step ② → Step ③ → Step ④ → Step ⑤         │
│  知识点    作业类型   理论/信息   提示词    三层次作业   │
│                                                       │
│  ┌────────────────────────────────────────────────┐  │
│  │         当前步骤内容区域                         │  │
│  │                                                 │  │
│  │  Step 1: 搜索/选择知识点                         │  │
│  │  ┌──────────────────────────────────────┐      │  │
│  │  │  🔍 [搜索框，输入关键词过滤]           │      │  │
│  │  │  ┌────┐ ┌────┐ ┌────┐ ┌────┐       │      │  │
│  │  │  │有理│ │实数│ │整式│ │因式│  ...   │      │  │
│  │  │  │ 数 │ │   │ │加减│ │分解│        │      │  │
│  │  │  └────┘ └────┘ └────┘ └────┘       │      │  │
│  │  └──────────────────────────────────────┘      │  │
│  │                                                 │  │
│  │            [ ← 上一步 ]  [ 下一步 → ]            │  │
│  └────────────────────────────────────────────────┘  │
│                                                       │
└──────────────────────────────────────────────────────┘
```

### 各步骤详细设计

#### Step 1 — 选择知识点
- 搜索框实时过滤 41 个知识点
- 知识点卡片网格展示，点击选中高亮
- 显示已选知识点

#### Step 2 — 选择作业类型
- 4 张卡片：潜能适配型 / 认知支持型 / 动态交互型 / 协作探究型
- 每张卡片展示：名称、概述、适用场景、输出形态
- 点击选中，高亮显示

#### Step 3 — 选择理论与补充信息
- 根据 Step 2 选择的作业类型，动态显示可选理论下拉框
- 动态显示对应的补充信息输入框（年级、学科、原始题目、教学目标等）
- 表单验证

#### Step 4 — 生成并确认提示词
- 点击"生成提示词"按钮 → 调用 `/api/generate-prompt`
- 左侧显示完整提示词文本（可编辑 textarea）
- 右侧显示作业类型说明和理论框架参考
- 确认后进入下一步
- 支持"返回修改"

#### Step 5 — 查看/调整三层次作业
- 点击"生成作业"→ 调用 `/api/generate-homework`（调 DeepSeek API，有 loading 状态）
- 三个 Tab 面板切换：远端发展区 | 最近发展区 | 现有发展区
- 每个面板显示该层级的作业内容（Markdown 渲染）
- 操作按钮：
  - 📝 **编辑**：将内容变为可编辑 textarea，教师手动调整
  - 🔄 **重新生成**：弹出输入框填写调整要求 → 调用 `/api/regenerate-level`
  - ✅ **确认**：该层级确认无误
- 底部汇总区域显示三个层级确认状态
- 最终导出按钮（可导出为 Markdown/文本）

---

## 七、四种作业类型的 ZPD 三层次输出规则

### 7.1 潜能适配型

| ZPD 层级 | 作业内容 | 数量 |
|---|---|---|
| 远端发展区 | 后继知识探索题（下一步学什么） | 3 道 |
| 最近发展区 | 当前知识点的认知进阶递进题（由浅入深） | 3 道 |
| 现有发展区 | 知识点讲解 + 基础题 | 知识讲解 + 1 道题 |

### 7.2 认知支持型

| ZPD 层级 | 支架强度 | 内容 |
|---|---|---|
| 远端发展区 | 完整支架 | 题干 + 四步骤支架问题 + 四步骤支架答案 + 反思问题 |
| 最近发展区 | 中等支架 | 题干 + 两步骤支架问题（制订计划 + 执行计划）+ 反思问题 |
| 现有发展区 | 最低支架 | 题干 + 1 个启发性提示 + 可展开查看的完整支架 |

### 7.3 动态交互型

| ZPD 层级 | 对话规则强度 | 内容 |
|---|---|---|
| 远端发展区 | 仅启发追问 | 作业题目 + 智能体只做追问/澄清，不提供提示 |
| 最近发展区 | 追问 + 方向提示 | 作业题目 + 智能体追问 + 方向性提示（首次受阻时） |
| 现有发展区 | 追问 + 提示 + 示例 | 作业题目 + 智能体追问 + 步骤提示 + 类题示例 |

### 7.4 协作探究型

| ZPD 层级 | 参与角色 | 内容 |
|---|---|---|
| 远端发展区 | 5 个角色完整协作 | 完整多角色工作流 + 全部三个阶段 |
| 最近发展区 | 3 个关键角色 | 问题澄清 + 证据质疑 + 反思反馈（精简流程） |
| 现有发展区 | 2 个角色 | 问题澄清 + 反思反馈（引导为主） |

---

## 八、DeepSeek 调用的系统提示词设计

整体上，每次调用 DeepSeek 使用两层提示词：

### 8.1 System Prompt（系统提示词）

根据作业类型固定：
```
你是一位资深的中小学数学教育专家，精通最近发展区理论和
[所选理论名称]。你的任务是按照指定格式为不同学习层级的
学生生成差异化作业内容。你生成的题目必须：
1. 学科知识准确无误
2. 难度梯度合理，符合ZPD各层级要求
3. 语言表述清晰，适合[所选年级]学生阅读
4. 严格按照要求的输出格式生成
```

### 8.2 User Prompt（用户提示词）

即 Step 4 生成并经教师确认后的完整提示词。

---

## 九、Python 依赖 (`requirements.txt`)

```
flask==3.1.0
openai==1.68.0
python-dotenv==1.1.0
```

---

## 十、实施步骤

### 第 1 步：创建 demo 目录结构
- 创建 `demo/` 文件夹及所有子目录
- 创建 `.env` 和 `.env.example` 文件
- 编写 `requirements.txt`

### 第 2 步：编写配置模块 (`config.py`)
- 结构化定义 41 个知识点
- 结构化定义 4 种作业类型及其可选理论
- 定义每种作业类型的补充信息字段
- 定义三种 ZPD 层级的作业规则

### 第 3 步：编写提示词构建模块 (`prompt_builder.py`)
- 根据作业类型加载对应提示词模板
- 将教师的输入（知识点、理论、补充信息）填充到模板中
- 生成 System Prompt 和 User Prompt

### 第 4 步：编写 DeepSeek 调用模块 (`llm_client.py`)
- 封装 OpenAI 兼容的 DeepSeek API 调用
- 从 `.env` 读取 API Key
- 错误处理和重试逻辑

### 第 5 步：编写作业生成模块 (`homework_generator.py`)
- 根据作业类型三层次规则构造请求
- 调用 `llm_client.py` 分别生成三层作业
- 解析和格式化 AI 返回结果

### 第 6 步：编写 Flask 主应用 (`app.py`)
- 实现 5 个 API 路由
- 集成所有模块
- 静态文件服务

### 第 7 步：编写前端界面 (`templates/index.html` + `static/`)
- 5 步向导 UI
- 知识点搜索/选择
- 作业类型卡片选择
- 动态表单
- 提示词确认编辑
- 三层次 Tab 查看 + 编辑/重生成

### 第 8 步：测试完整流程
- 启动 `python app.py`
- 浏览器访问 localhost:5000
- 走通完整流程

---

## 十一、扩展方向（未来迭代）

- [ ] 支持保存/加载历史作业设计
- [ ] 教师账号系统
- [ ] 导出为 PDF/Word
- [ ] 接入更多大模型（如 GPT、Claude）
- [ ] 学情数据批量导入（CSV）
- [ ] 作业发布到在线平台

---

> **最后更新**：2026-08-05（已完成实施）

---

## 十二、实施完成状态

### 已完成文件清单

```
demo/
├── .env                              # ✅ DeepSeek API Key 配置文件（不随项目分发）
├── .env.example                      # ✅ API Key 配置模板
├── .gitignore                        # ✅ Git 忽略规则
├── requirements.txt                  # ✅ Python 依赖
├── setup.bat                         # ✅ Windows 一键初始化脚本
├── setup.sh                          # ✅ macOS/Linux 一键初始化脚本
├── app.py                            # ✅ Flask 主应用（5 个 API 路由 + 1 个页面路由）
├── config.py                         # ✅ 配置模块（学段/学科/知识点 + 4 作业类型 + ZPD 层级）
├── prompt_builder.py                 # ✅ 提示词构建（4 种类型模板 + 12 组 ZPD 层级指令）
├── llm_client.py                     # ✅ DeepSeek API 封装（OpenAI 兼容接口）
├── homework_generator.py             # ✅ 三层次作业生成（并行调用 + 单独重生成）
├── templates/
│   └── index.html                    # ✅ 前端页面（5 步向导式界面）
└── static/
    ├── style.css                     # ✅ 样式（响应式设计，支持移动端）
    └── app.js                        # ✅ 前端交互逻辑（步骤导航/API调用/编辑/重生成）
```

### 环境信息

- **Python 版本**：3.12.13（由 `uv venv` 创建）
- **虚拟环境**：`demo/.venv/`（使用 `uv` 管理）
- **包管理器**：uv（Rust 实现，比 pip 快 10-100 倍）

### 启动方式

```bash
cd demo

# 1. 创建虚拟环境（仅首次，已完成）
uv venv

# 2. 安装依赖（仅首次，已完成）
uv pip install flask openai python-dotenv

# 3. 编辑 .env，填入真实的 DeepSeek API Key（已完成）
#    DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx

# 4. 启动
.venv/Scripts/python app.py
# 浏览器访问 http://localhost:5000
```

### API 验证结果

| 端点 | 方法 | 状态 | 说明 |
|---|---|---|---|
| `/` | GET | ✅ 200 | 主页面正常加载 |
| `/api/config` | GET | ✅ 200 | 返回知识点、作业类型、API 状态 |
| `/api/generate-prompt` | POST | ✅ 200 | 生成完整提示词（不调用 AI） |
| `/api/generate-homework` | POST | ✅ 就绪 | 调用 DeepSeek 生成三层次作业（API Key 已配置） |
| `/api/regenerate-level` | POST | ✅ 就绪 | 重新生成单个层级（API Key 已配置） |
| `/api/check-api` | GET | ✅ 200 | API Key 已配置，模型 deepseek-chat |

### 注意

- 提示词生成功能（`/api/generate-prompt`）无需 API Key 即可使用
- 作业内容生成功能（`/api/generate-homework`、`/api/regenerate-level`）需要先在 `demo/.env` 中配置有效的 DeepSeek API Key
- 前端会实时显示 API 配置状态（右下角徽章）
- 支持教师在 Step 4 编辑提示词、Step 5 编辑作业内容
- 三个 ZPD 层级并行调用 DeepSeek，提高生成速度

---

## 十三、迁移指南

### 迁移时需要复制/发送的文件

将整个 `demo/` 文件夹复制到目标电脑，以下文件会被使用：

| 文件 | 用途 | 备注 |
|---|---|---|
| `setup.bat` / `setup.sh` | 一键初始化脚本 | 新电脑上首先运行 |
| `requirements.txt` | Python 依赖清单 | setup 脚本自动安装 |
| `.env.example` | API Key 配置模板 | setup 脚本会自动复制为 `.env` |
| `.gitignore` | Git 忽略规则 | 可选 |
| `app.py` | Flask 主应用 | — |
| `config.py` | 配置模块 | — |
| `prompt_builder.py` | 提示词构建 | — |
| `llm_client.py` | LLM 客户端 | — |
| `homework_generator.py` | 作业生成 | — |
| `templates/index.html` | 前端页面 | — |
| `static/style.css` | 样式 | — |
| `static/app.js` | 前端交互 | — |

> ⚠️ **不要复制**以下目录/文件，它们将在新机器上重新生成：
> - `.venv/` — 虚拟环境（含硬编码路径，跨机器不可用）
> - `.env` — 含 API Key，应在新机器上重新配置
> - `__pycache__/` — Python 缓存

### 在新电脑上的操作步骤

**前置条件**：目标电脑需安装 [uv](https://docs.astral.sh/uv/getting-started/installation/)。

#### Windows

```
1. 将 demo 文件夹复制到目标电脑任意位置
2. 双击 demo/setup.bat
3. 编辑 demo/.env，填入你的 DeepSeek API Key
4. 双击运行，或在终端执行：
   cd demo
   .venv\Scripts\python app.py
5. 浏览器访问 http://localhost:5000
```

#### macOS / Linux

```bash
# 1. 将 demo 文件夹复制到目标电脑任意位置
# 2. 运行初始化脚本
cd demo
bash setup.sh

# 3. 编辑 .env，填入你的 DeepSeek API Key
# DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx

# 4. 启动
.venv/bin/python app.py

# 5. 浏览器访问 http://localhost:5000
```

#### 手动初始化（不使用 setup 脚本）

```bash
cd demo
uv venv
uv pip install -r requirements.txt
cp .env.example .env
# 编辑 .env 填入 API Key
.venv/bin/python app.py   # macOS/Linux
.venv\Scripts\python app.py  # Windows
```

---

## 十四、通过 Git 分享项目

### 当前状态

`demo/` 目录已初始化为 Git 仓库，`.gitignore` 已配置排除以下内容：

| 已排除 | 原因 |
|---|---|
| `.venv/` | 虚拟环境，跨机器不可用 |
| `.env` | 含 API Key，敏感信息 |
| `__pycache__/` | Python 缓存 |

### 推送到 GitHub / GitLab

```bash
cd demo

# 1. 在 GitHub/GitLab 上创建一个新仓库（不要勾选 README/.gitignore）
#    记下仓库地址，例如：https://github.com/你的用户名/zpd-homework-tool.git

# 2. 添加远程仓库并推送
git remote add origin https://github.com/你的用户名/zpd-homework-tool.git
git branch -M main
git push -u origin main
```

### 其他人使用方式

```bash
# 1. 克隆
git clone https://github.com/你的用户名/zpd-homework-tool.git
cd zpd-homework-tool

# 2. 一键初始化（Windows 双击 setup.bat，Mac/Linux 执行 bash setup.sh）

# 3. 编辑 .env 填入 DeepSeek API Key

# 4. 启动
.venv\Scripts\python app.py    # Windows
.venv/bin/python app.py         # Mac/Linux
```

---

> **最后更新**：2026-08-05（新增迁移指南 + 维护规则 + 初始化脚本 + Git 分享）
